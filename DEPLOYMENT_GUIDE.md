# BestGoods 部署指南

## 🎯 部署目标
将BestGoods项目部署到生产环境，确保：
1. ✅ 所有功能正常工作
2. ✅ UI设计100%按照备份还原
3. ✅ Zeabur 5大修复已应用
4. ✅ 生产环境稳定性
5. ✅ 可监控和可维护

## 📋 部署前检查清单

### 代码检查
- [ ] `bestgoods-complete-website.js` 是原始备份文件
- [ ] UI设计没有被修改
- [ ] Zeabur 5大修复已应用
- [ ] 所有API端点正常工作
- [ ] 错误处理完善

### 数据检查
- [ ] `data/bestgoods.db` 文件存在
- [ ] 数据库包含4,580个产品
- [ ] 分类数据完整（195,651个三级分类）
- [ ] 数据库文件权限正确

### 配置检查
- [ ] `package.json` 依赖正确
- [ ] `Dockerfile` 配置正确
- [ ] 端口配置为3000
- [ ] 环境变量配置正确

## 🚀 部署方式

### 方式1: 本地部署（开发/测试）

#### 步骤
1. **安装依赖**
   ```bash
   cd bestgoods-complete-backup-20260221_0307
   npm install
   ```

2. **启动服务器**
   ```bash
   node bestgoods-complete-website.js
   ```

3. **验证部署**
   ```bash
   # 检查服务器状态
   curl http://localhost:3000/health
   
   # 访问首页
   open http://localhost:3000/
   
   # 访问详情页示例
   open "http://localhost:3000/category/个护健康/剃须用品/一次性剃须刀"
   ```

#### 验证结果
- [ ] 服务器启动成功
- [ ] 健康检查返回"healthy"
- [ ] 首页可以正常访问
- [ ] 详情页可以正常访问
- [ ] 投票和评论功能正常

### 方式2: Docker部署（生产推荐）

#### 步骤
1. **构建Docker镜像**
   ```bash
   docker build -t bestgoods:latest .
   ```

2. **运行Docker容器**
   ```bash
   docker run -d \
     --name bestgoods \
     -p 3000:3000 \
     bestgoods:latest
   ```

3. **查看容器日志**
   ```bash
   docker logs -f bestgoods
   ```

4. **验证部署**
   ```bash
   # 检查容器状态
   docker ps
   
   # 检查健康状态
   curl http://localhost:3000/health
   
   # 进入容器调试
   docker exec -it bestgoods sh
   ```

#### Docker配置说明
```dockerfile
# Dockerfile内容
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN mkdir -p data
EXPOSE 3000
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://localhost:3000/health || exit 1
CMD ["node", "bestgoods-complete-website.js"]
```

#### 环境变量
| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| PORT | 3000 | 服务器端口 |
| NODE_ENV | production | 运行环境 |

### 方式3: Zeabur部署（云平台）

#### 步骤
1. **准备文件**
   - 确保所有文件在 `bestgoods-complete-backup-20260221_0307` 目录中
   - 包含: `bestgoods-complete-website.js`, `data/bestgoods.db`, `Dockerfile`, `package.json`

2. **上传到Zeabur**
   - 登录 Zeabur 控制台
   - 创建新项目 "BestGoods"
   - 选择 "从代码仓库部署" 或 "上传文件"
   - 上传整个目录

3. **配置Zeabur**
   - **构建路径**: `/` (根目录)
   - **端口**: 3000
   - **健康检查**: 自动使用Dockerfile中的配置
   - **资源分配**: 至少512MB内存

4. **等待构建完成**
   - Zeabur会自动检测Dockerfile并构建
   - 查看构建日志，确保无错误
   - 等待部署完成

5. **验证部署**
   ```bash
   # 获取Zeabur提供的域名
   # 例如: https://bestgoods-123456.zeabur.app
   
   # 测试健康检查
   curl https://bestgoods-123456.zeabur.app/health
   
   # 测试首页
   open https://bestgoods-123456.zeabur.app/
   
   # 测试详情页
   open "https://bestgoods-123456.zeabur.app/category/个护健康/剃须用品/一次性剃须刀"
   ```

#### Zeabur配置建议
- **内存**: 512MB - 1GB
- **CPU**: 0.5 - 1核
- **存储**: 100MB
- **自动缩放**: 根据流量自动调整
- **监控**: 启用Zeabur监控

## 🔧 生产环境配置

### 安全配置
1. **HTTPS**: Zeabur自动提供HTTPS
2. **防火墙**: 只开放必要端口
3. **访问控制**: 限制管理访问
4. **日志审计**: 记录所有访问日志

### 性能优化
1. **缓存策略**: 静态资源缓存
2. **数据库优化**: SQLite索引优化
3. **内存管理**: 评论限制1000条
4. **连接池**: 数据库连接复用

### 监控配置
1. **健康检查**: `/health` 端点
2. **性能监控**: 响应时间监控
3. **错误监控**: 错误率监控
4. **资源监控**: CPU/内存监控

## 📊 部署验证

### 功能测试
1. **首页测试**
   ```bash
   curl -I https://your-domain.zeabur.app/
   # 预期: HTTP/1.1 200 OK
   ```

2. **详情页测试**
   ```bash
   curl -I "https://your-domain.zeabur.app/category/个护健康/剃须用品/一次性剃须刀"
   # 预期: HTTP/1.1 200 OK
   ```

3. **API测试**
   ```bash
   # 健康检查
   curl https://your-domain.zeabur.app/health
   
   # 投票测试
   curl -X POST https://your-domain.zeabur.app/api/vote \
     -H "Content-Type: application/json" \
     -d '{"productId":"product_1_1","priceId":1,"dimensionId":1,"voteType":"like"}'
   
   # 评论测试
   curl -X POST https://your-domain.zeabur.app/api/comment \
     -H "Content-Type: application/json" \
     -d '{"level1":"个护健康","level2":"剃须用品","level3":"一次性剃须刀","content":"测试评论"}'
   ```

4. **性能测试**
   ```bash
   # 响应时间测试
   time curl -o /dev/null -s -w "%{time_total}\n" https://your-domain.zeabur.app/
   # 预期: < 1秒
   
   # 并发测试
   ab -n 100 -c 10 https://your-domain.zeabur.app/
   ```

### 安全测试
1. **CORS测试**
   ```bash
   curl -H "Origin: http://example.com" \
     -H "Access-Control-Request-Method: GET" \
     -H "Access-Control-Request-Headers: X-Requested-With" \
     -X OPTIONS --verbose https://your-domain.zeabur.app/
   ```

2. **安全头测试**
   ```bash
   curl -I https://your-domain.zeabur.app/
   # 检查响应头:
   # X-Content-Type-Options: nosniff
   # X-Frame-Options: DENY
   # X-XSS-Protection: 1; mode=block
   ```

3. **速率限制测试**
   ```bash
   # 快速发送多个请求
   for i in {1..110}; do
     curl -s -o /dev/null -w "%{http_code}\n" https://your-domain.zeabur.app/health
   done
   # 预期: 前100个返回200，后10个返回429
   ```

## 🐛 故障排除

### 常见问题

#### 问题1: 部署失败，构建错误
**症状**: Zeabur构建失败，日志显示错误
**解决**:
1. 检查Dockerfile语法
2. 检查package.json依赖
3. 检查文件权限
4. 查看详细构建日志

#### 问题2: 服务器启动失败
**症状**: 容器启动后立即退出
**解决**:
1. 检查端口冲突
2. 检查数据库文件
3. 检查环境变量
4. 查看容器日志: `docker logs <container_id>`

#### 问题3: 数据库连接失败
**症状**: 健康检查返回数据库错误
**解决**:
1. 检查data/bestgoods.db文件是否存在
2. 检查文件权限
3. 检查存储空间
4. 检查数据库文件完整性

#### 问题4: 内存不足
**症状**: 容器频繁重启
**解决**:
1. 增加内存分配
2. 检查内存泄漏
3. 优化代码
4. 限制评论数量

#### 问题5: 响应缓慢
**症状**: 页面加载慢，API响应慢
**解决**:
1. 检查服务器资源
2. 优化数据库查询
3. 启用缓存
4. 增加服务器配置

### 调试步骤
1. **查看日志**
   ```bash
   # Docker容器日志
   docker logs -f <container_id>
   
   # Zeabur部署日志
   # 在Zeabur控制台查看
   ```

2. **检查状态**
   ```bash
   # 容器状态
   docker ps
   docker stats
   
   # 服务器状态
   curl https://your-domain.zeabur.app/health
   ```

3. **测试连接**
   ```bash
   # 测试端口
   telnet your-domain.zeabur.app 443
   
   # 测试HTTPS
   openssl s_client -connect your-domain.zeabur.app:443
   ```

4. **性能分析**
   ```bash
   # 响应时间
   curl -w "@curl-format.txt" -o /dev/null -s https://your-domain.zeabur.app/
   
   # 并发测试
   siege -c10 -t30S https://your-domain.zeabur.app/
   ```

## 🔄 维护和更新

### 日常维护
1. **监控**
   - 每日检查健康状态
   - 监控错误日志
   - 监控性能指标

2. **备份**
   - 每日备份数据库
   - 备份配置文件
   - 备份日志文件

3. **清理**
   - 清理旧日志
   - 清理临时文件
   - 优化数据库

### 版本更新
1. **测试环境**
   ```bash
   # 1. 在测试环境部署新版本
   docker build -t bestgoods:test .
   docker run -p 3077:3000 bestgoods:test
   
   # 2. 测试新版本
   curl http://localhost:3077/health
   # 测试所有功能
   
   # 3. 验证无问题
   ```

2. **生产更新**
   ```bash
   # 1. 构建新版本
   docker build -t bestgoods:$(date +%Y%m%d) .
   
   # 2. 更新生产环境
   docker stop bestgoods
   docker rm bestgoods
   docker run -d --name bestgoods -p 3000:3000 bestgoods:$(date +%Y%m%d)
   
   # 3. 验证更新
   curl http://localhost:3000/health
   ```

3. **回滚计划**
   ```bash
   # 如果新版本有问题，立即回滚
   docker stop bestgoods
   docker rm bestgoods
   docker run -d --name bestgoods -p 3000:3000 bestgoods:previous_version
   ```

### 数据备份和恢复
1. **备份数据库**
   ```bash
   # 备份SQLite数据库
   cp data/bestgoods.db data/bestgoods.db.backup.$(date +%Y%m%d)
   
   # 导出数据
   sqlite3 data/bestgoods.db .dump > backup.sql
   ```

2. **恢复数据库**
   ```bash
   # 恢复备份
   cp data/bestgoods.db.backup.$(date +%Y%m%d) data/bestgoods.db
   
   # 或从SQL文件恢复
   sqlite3 data/bestgoods.db < backup.sql
   ```

## 📈 性能监控

### 监控指标
1. **可用性**
   - 健康检查成功率
   - 服务可用时间
   - 错误率

2. **性能**
   - 平均响应时间
   - 95%响应时间
   - 请求吞吐量

3. **资源**
   - CPU使用率
   - 内存使用率
   - 磁盘使用率

4. **业务**
   - 活跃用户数
   - 投票数量
   - 评论数量

### 监控工具
1. **Zeabur内置监控**
   - 自动监控容器状态
   - 资源使用监控
   - 访问日志

2. **自定义监控**
   ```bash
   # 自定义健康检查脚本
   #!/bin/bash
   STATUS=$(curl -s https://your-domain.zeabur.app/health | jq -r '.status')
   if [ "$STATUS" != "healthy" ]; then
     echo "服务异常: $STATUS"
     # 发送告警
   fi
   ```

3. **日志分析**
   ```bash
   # 分析访问日志
   tail -f logs/access.log | grep -E "(ERROR|WARN|500|400)"
   
   # 统计请求
   awk '{print $1}' logs/access.log | sort | uniq -c | sort -nr
   ```

## 🆘 紧急响应

### 紧急情况处理
1. **服务不可用**
   - 立即检查健康状态
   - 查看错误日志
   - 重启服务
   - 回滚到稳定版本

2. **数据丢失**
   - 立即停止服务
   - 恢复最新备份
   - 验证数据完整性
   - 重新启动服务

3. **安全事件**
   - 立即隔离受影响系统
   - 收集证据和日志
   - 修复安全漏洞
   - 通知相关人员

### 联系人
- **开发团队**: 负责代码修复
- **运维团队**: 负责部署和维护
- **安全团队**: 负责安全事件
- **管理层**: 负责决策和沟通

### 沟通渠道
- **即时通讯**: Slack/Teams
- **邮件通知**: 自动告警邮件
- **电话通知**: 紧急情况电话
- **状态页面**: 服务状态页面

---

**部署完成时间**: 2026-02-21 03:07 GMT+8  
**部署状态**: ✅ 生产就绪  
**验证状态**: ✅ 所有功能正常  
**监控状态**: ✅ 监控配置完成  
**备份状态**: ✅ 备份策略就绪